# OpenCode Adapter

Use this repository as a portable skill registry:

- `AGENTS.md` for repository entry guidance
- `skills/` for workflow definitions
- `starter-packs/` for opinionated adoption paths
- `registry/assets.json` for machine-readable discovery
