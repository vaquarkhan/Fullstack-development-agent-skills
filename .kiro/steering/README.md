# Kiro Steering Adapter

This directory adapts the fullstack lifecycle for Kiro steering workflows.

Primary entry:

- `skills/using-fullstack-agent-skills/SKILL.md`

Recommended command flow:

- `/spec` -> `/plan` -> `/build` -> `/validate` -> `/review` -> `/ship`
