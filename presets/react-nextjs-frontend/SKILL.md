---
name: react-nextjs-frontend
description: Apply frontend guidance for React and Next.js stacks, including routing, rendering strategy, state boundaries, and performance defaults. Use when a project uses React, Next.js, TypeScript, and component-driven UI delivery.
disable-model-invocation: true
---

# React Next.js Frontend Preset

## Defaults

- TypeScript strict mode
- Server components by default, client components only when needed
- Centralized design tokens and UI primitives
- Route-level loading and error states
- Accessibility checks in pull request flow

## Recommended Pairings

- `ui-engineering-and-design-systems`
- `api-contract-first-development`
