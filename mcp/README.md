# MCP Templates

Starter MCP configuration templates for fullstack delivery workflows.

- `github.json` — issues, PRs, and release metadata
- `postgres.json` — relational data inspection
- `aws.json` — AWS operational tooling
- `azure.json` — Azure operational tooling
- `gcp.json` — GCP operational tooling

Replace placeholders before connecting live environments.
