# Copilot Instructions

Use this repository as a fullstack skill toolkit:

- Start with `AGENTS.md`
- Follow lifecycle command docs in `.cursor/commands/`
- Route to skills in `skills/` and presets in `presets/`
- Validate against references in `references/`
